import React from 'react'

const Router = () => {
  return (
    <div>
      <h1>Intro</h1>
    </div>
  )
}

export default Router
/***
 * When we have to show mulitple page web we use react router v6
 * if we use ancer tag our web page will reload 
 * If we use react router our page and content will change but page will not refresh
 * 
 */